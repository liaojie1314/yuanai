import type { ReactElement } from 'react'

/** 登录窗口的基础界面。 */
export function App(): ReactElement {
  return (
    <main aria-label="登录元AI" tabIndex={-1}>
      <h1>登录元AI</h1>
      <p>登录表单正在准备中。</p>
    </main>
  )
}
