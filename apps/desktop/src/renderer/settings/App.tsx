import type { ReactElement } from 'react'

/** 设置窗口的基础界面。 */
export function App(): ReactElement {
  return (
    <main aria-label="元AI 设置" tabIndex={-1}>
      <h1>设置</h1>
      <p>设置项正在准备中。</p>
    </main>
  )
}
