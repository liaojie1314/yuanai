#!/bin/sh

echo "⏳ Running type check..."
corepack pnpm typecheck || { echo "❌ Type check failed. Push aborted."; exit 1; }

echo "⏳ Running unit tests..."
corepack pnpm test:unit || { echo "❌ Unit tests failed. Push aborted."; exit 1; }

echo "✅ All checks passed. Pushing..."
