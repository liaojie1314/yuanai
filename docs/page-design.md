# 页面设计文档 — 元AI (yuanai)

**面向对象**: UI 设计师（OpenDesign / Penpot 原型设计）  
**设计工具**: OpenDesign / Penpot  
**版本**: v1.1  
**最后更新**: 2026-06-27

---

## 一、设计理念

### 核心原则

1. **清透感**：主色调为淡蓝，背景微蓝白，整体清爽通透，不沉重
2. **内容优先**：对话内容是主角，UI 退居辅助，避免喧宾夺主
3. **无卡片化**：摒弃后台管理风格（无明显 border 卡片）——用背景色层次和留白区分区域，而非边框
4. **有活力**：适量动画让页面有生命感，包括背景微动、交互反馈、AI 打字效果
5. **克制圆角**：圆角统一 8px（输入框/按钮/气泡），强调精致而非夸张

### 设计风格参考

- 整体风格参考：Linear、Notion、Claude.ai（淡色、无卡片、干净）
- 颜色感受参考：Figma 新版（清透淡蓝）+ Tailwind CSS 官网
- **不要参考**：企业后台管理系统、Ant Design Pro 默认样式

---

## 二、全局设计规范

### 颜色系统

#### 品牌蓝（主色调）

| 名称     | 色值      | 用途                           |
| -------- | --------- | ------------------------------ |
| Blue 600 | `#2563EB` | 按钮按下态、链接 hover         |
| Blue 500 | `#3B82F6` | **主按钮**、活跃状态、图标强调 |
| Blue 400 | `#60A5FA` | 渐变辅色、次要强调             |
| Blue 200 | `#BFDBFE` | 边框淡化、分割线               |
| Blue 50  | `#EFF6FF` | 选中行背景、标签背景           |

#### 背景层次（从深到浅）

| 层次      | 色值      | 用途                   |
| --------- | --------- | ---------------------- |
| 侧边栏    | `#F0F6FF` | 左侧会话列表背景       |
| 页面底    | `#F4F8FF` | 整体页面背景           |
| 面板/浮层 | `#FFFFFF` | 弹窗、Dropdown、输入区 |
| 高亮背景  | `#EBF3FF` | hover/选中状态         |

**注意**：以上背景层次均微带蓝色调，绝不使用纯灰色（`#F5F5F5`），保持品牌统一感。

#### 文字颜色

| 名称   | 色值      | 用途                         |
| ------ | --------- | ---------------------------- |
| 主文字 | `#1A2540` | 正文、标题（深蓝黑，非纯黑） |
| 次文字 | `#5A6A8A` | 说明、时间戳、Label          |
| 弱文字 | `#9BABC5` | 占位符、禁用态               |
| 反色   | `#FFFFFF` | 蓝色背景上的文字             |

#### 功能色

| 名称 | 色值      | 用途                   |
| ---- | --------- | ---------------------- |
| 成功 | `#10B981` | 表单校验通过、成功提示 |
| 警告 | `#F59E0B` | 警告提示               |
| 错误 | `#EF4444` | 表单校验失败、错误提示 |
| 信息 | `#3B82F6` | 信息提示（复用品牌蓝） |

#### 深色主题覆盖

| 变量     | 深色值    |
| -------- | --------- |
| 侧边栏   | `#0F1829` |
| 页面底   | `#111827` |
| 面板     | `#1C2A40` |
| 高亮背景 | `#1E3A5F` |
| 主文字   | `#E8F0FE` |
| 次文字   | `#8BA3C7` |

---

### 字体规范

**字体栈**（按优先级）：

```
PingFang SC → Hiragino Sans GB → Microsoft YaHei → -apple-system → sans-serif
```

| 用途               | 大小 | 行高 | 字重 | 颜色     |
| ------------------ | ---- | ---- | ---- | -------- |
| 大标题（页面 H1）  | 28px | 36px | 700  | 主文字   |
| 区块标题（H2）     | 18px | 26px | 600  | 主文字   |
| 正文               | 15px | 24px | 400  | 主文字   |
| 次要说明           | 13px | 20px | 400  | 次文字   |
| 按钮文字           | 14px | 20px | 500  | 随背景   |
| 标签/徽章          | 12px | 16px | 500  | 次文字   |
| 代码               | 13px | 20px | 400  | 等宽字体 |
| 输入框 placeholder | 15px | —    | 400  | 弱文字   |

---

### 圆角规范

| 场景          | 圆角值               | 说明                       |
| ------------- | -------------------- | -------------------------- |
| 输入框        | 8px                  | 登录/注册/搜索等所有输入框 |
| 普通按钮      | 8px                  | 主按钮、次要按钮           |
| 图标按钮      | 8px（正方形）        | 工具栏图标按钮             |
| 用户消息气泡  | `14px 14px 4px 14px` | 右下角尖，体现"说话"感     |
| AI 消息气泡   | 无气泡（透明背景）   | 直接渲染文本               |
| Dropdown/弹窗 | 10px                 | 模型选择器、右键菜单       |
| Modal 对话框  | 12px                 | 重命名、确认弹窗           |
| 头像          | 50%（圆形）          | 用户/AI 头像               |
| 标签/徽章     | 4px                  | 模型标签、状态标签         |

---

### 间距规范

**基础单位：4px**

| 变量 | 值   | 常见用途                 |
| ---- | ---- | ------------------------ |
| xs   | 4px  | 图标与文字间距           |
| sm   | 8px  | 表单元素内边距           |
| md   | 12px | 按钮内水平边距           |
| lg   | 16px | 卡片内边距、列表项内边距 |
| xl   | 24px | 区块间距                 |
| 2xl  | 32px | 大区块间距               |
| 3xl  | 48px | 页面级间距               |

---

### 阴影规范

阴影尽量克制，仅用于浮层，**不用于普通卡片**。

| 场景          | 阴影值                            |
| ------------- | --------------------------------- |
| 输入框 focus  | `0 0 0 3px rgba(59,130,246,0.2)`  |
| Dropdown/弹窗 | `0 8px 24px rgba(30,64,175,0.12)` |
| 悬浮工具提示  | `0 4px 12px rgba(30,64,175,0.1)`  |
| 禁止使用      | 普通列表项、侧边栏、内容区域      |

---

### 动画规范

| 场景                   | 类型                                              | 时长     | 缓动                             |
| ---------------------- | ------------------------------------------------- | -------- | -------------------------------- |
| 按钮 hover 缩放        | `scale(1.02)`                                     | 150ms    | `ease-out`                       |
| 按钮 active 按下       | `scale(0.97)`                                     | 80ms     | `ease-in`                        |
| 输入框 focus 光晕      | opacity 0→1                                       | 200ms    | `ease`                           |
| 侧边栏折叠/展开        | `translateX`                                      | 280ms    | `cubic-bezier(0.4,0,0.2,1)`      |
| 页面路由切换           | `opacity 0→1` + `translateY(6px→0)`               | 220ms    | `ease-out`                       |
| 消息出现               | `opacity 0→1` + `translateY(8px→0)`               | 250ms    | `ease-out`                       |
| AI 打字光标            | 光标闪烁                                          | 600ms    | `steps(1)` 循环                  |
| 背景浮动元素（认证页） | 上下浮动 `y: -8px→8px`                            | 4-6s     | `ease-in-out` 循环               |
| 密码强度条填充         | 宽度变化                                          | 300ms    | `ease-out`                       |
| Toast 提示出现         | `translateY(100%→0)`                              | 250ms    | `cubic-bezier(0.4,0,0.2,1)`      |
| 模型选择 Dropdown      | `opacity 0→1` + `scaleY(0.95→1)`                  | 180ms    | `ease-out`                       |
| 语音录音波形           | 音量驱动 scaleY 波动                              | 实时     | 无缓动（实时跟随）               |
| 语音按钮激活           | `scale(1→1.15)` + 红色扩散光圈                    | 300ms    | `cubic-bezier(0.34,1.56,0.64,1)` |
| 语音转文字填充         | 文字逐词淡入                                      | 200ms/词 | `ease-out`                       |
| Tool 调用展开          | `height 0→auto` + `opacity 0→1`                   | 220ms    | `ease-out`                       |
| Tool 调用成功图标      | `scale(0→1)` 绿色勾                               | 200ms    | `cubic-bezier(0.34,1.56,0.64,1)` |
| Artifact 面板展开      | `translateX(100%→0)`                              | 300ms    | `cubic-bezier(0.4,0,0.2,1)`      |
| 推荐追问出现           | `opacity 0→1` + `translateY(4px→0)` 逐项延迟 40ms | 200ms    | `ease-out`                       |
| 停止生成按钮出现       | `scale(0.8→1)` + `opacity 0→1`                    | 180ms    | `ease-out`                       |

---

## 三、认证页面组

### 认证页公共背景（登录/注册/忘记密码共用）

**桌面端（宽屏 ≥ 1024px）：左右分栏布局**

```
┌──────────────────────┬──────────────────────┐
│                      │                      │
│   左侧装饰区（45%）  │   右侧表单区（55%）  │
│                      │                      │
│  背景：线性渐变       │  背景：#FFFFFF        │
│  from #1E40AF        │                      │
│  to   #3B82F6        │  表单内容居中         │
│                      │                      │
│  品牌 Logo + 口号    │                      │
│  浮动装饰图形        │                      │
│                      │                      │
└──────────────────────┴──────────────────────┘
```

**左侧装饰区设计要点**：

- 背景：`linear-gradient(145deg, #1E3A8A 0%, #1D4ED8 40%, #3B82F6 100%)`
- 顶部：品牌 Logo（白色「元」字 + 「yuanai」文字）
- 中部装饰（选其一）：
  - 方案 A：3-4 个对话气泡浮动动画，内容为 "你好" / "帮我写一段代码" / "解释一下量子纠缠" 等示意文字，气泡为半透明白色（`rgba(255,255,255,0.12)`），border `rgba(255,255,255,0.2)`
  - 方案 B：抽象几何图形（圆形、六边形轮廓），半透明白色线条，缓慢旋转或浮动
- 浮动动画：每个元素 y 轴 `-8px→8px` 来回浮动，各元素错开 0.5-1s 延迟，营造立体感
- 底部：3-4 条功能亮点文字（白色）例如「多模型支持 · GPT-4o · Claude · DeepSeek」
- 版权信息（弱化）：`rgba(255,255,255,0.4)`，12px

**移动端（< 1024px）：单栏，顶部带品牌色装饰**

```
┌────────────────────┐
│  品牌 Logo 区域    │  高度 200px
│  蓝色渐变背景      │  含浮动小图形
│  Logo + slogan    │
├────────────────────┤
│                    │
│  表单区域          │  圆角顶部 20px
│  白色背景          │  向上叠在渐变区
│                    │
└────────────────────┘
```

---

### 3.1 登录页 `/login`

#### 页面标题区

- 标题：「欢迎回来」，28px / 700 / `#1A2540`
- 副标题：「登录你的元AI账号继续对话」，14px / 400 / `#5A6A8A`
- 标题与表单间距：32px

#### 表单控件

**① 邮箱输入框**

```
Label:       「邮箱」，13px / 500 / #5A6A8A，距输入框上方 6px
Placeholder: your@email.com
高度:        48px
宽度:        100%（最大 380px）
边框:        1px solid #BFDBFE（默认）
             1px solid #3B82F6 + focus 光晕（聚焦）
             1px solid #EF4444（校验失败）
背景:        #FFFFFF
圆角:        8px
左侧图标:    Mail 图标，16px，#9BABC5（聚焦时变 #3B82F6，动画 150ms）
内边距:      左 40px（含图标位），右 16px
```

**② 密码输入框**

```
Label:       「密码」，同上
Placeholder: 请输入密码
高度:        48px
左侧图标:    Lock 图标，同上规则
右侧控件:    眼睛图标（显示/隐藏密码切换），20px，点击区域 40x40px
             状态：EyeOff（默认）/ Eye（点击后），颜色 #9BABC5
type:        password（默认）/ text（显示时）
```

**③ 辅助行（记住我 + 忘记密码）**

```
布局：       flex，两端对齐，距密码框下方 12px
左侧：       checkbox「记住我 7 天」
             - checkbox：14x14px，选中时蓝色填充（#3B82F6）
             - 文字：13px / #5A6A8A
右侧：       「忘记密码？」
             - 文字：13px / #3B82F6，无下划线，hover 时出现下划线
```

**④ 登录按钮**

```
文字:        「登录」
宽度:        100%
高度:        48px
圆角:        8px
背景:        #3B82F6（默认）→ #2563EB（hover）→ #1D4ED8（active）
文字:        16px / 600 / #FFFFFF
Loading 态:  文字替换为旋转 spinner（16px 白色圆环）+ 「登录中...」
禁用态:      背景 #BFDBFE，文字 #9BABC5，cursor not-allowed
动画:        hover scale(1.01)，active scale(0.98)，均 150ms ease
```

**⑤ 分割线**

```
布局:        「─────── 或使用以下方式登录 ───────」
线条:        1px / #EBF3FF
文字:        12px / #9BABC5，背景 #FFFFFF 遮挡线条
距离:        上下各 20px
```

**⑥ 第三方登录按钮**

三个按钮并排（若屏幕宽度不足则换为图标行）：

```
微信登录按钮:
  - 图标：微信绿色 Logo（24x24）
  - 文字：「微信登录」（移动端优先显示）
  - 背景：#FFFFFF，border 1px solid #BFDBFE
  - hover：背景 #EFF6FF，border #3B82F6
  - 高度：44px，圆角 8px

Google 登录:
  - 同上，Google 彩色 Logo

GitHub 登录:
  - 同上，GitHub 黑色 Logo（深色主题时反色）
```

**⑦ 注册引导**

```
位置：       表单最底部，居中
文字：       「还没有账号？」+ 「立即注册」
前者颜色：   #5A6A8A，13px
后者颜色：   #3B82F6，13px / 600，hover 下划线
```

#### 表单校验规则

| 字段     | 规则                    | 错误提示（红色，12px，输入框下方 4px）      |
| -------- | ----------------------- | ------------------------------------------- |
| 邮箱     | 必填；符合 `email` 格式 | 「请输入正确的邮箱地址」                    |
| 密码     | 必填；最少 8 位         | 「密码不能为空」/ 「密码至少 8 位」         |
| 登录失败 | 后端返回 401            | 输入框变红边框 + 「邮箱或密码错误，请重试」 |

- 校验时机：字段 **失焦（onBlur）** 时触发，而非每次按键
- 成功态：登录按钮 loading 1.5s → 整页淡出 → 进入主界面（`opacity 0→1`，220ms）

---

### 3.2 注册页 `/register`

#### 表单控件（在登录页基础上新增/变化）

**① 用户名输入框**（位于邮箱之前）

```
Label:       「用户名」
Placeholder: 2-20 位，支持中英文和数字
右侧实时反馈:
  - 输入时：实时调用 API 检查用户名是否已被注册
  - loading：右侧小 spinner
  - 可用：绿色勾（CheckCircle 16px，#10B981）
  - 已占用：红色 X（XCircle 16px，#EF4444）+ 提示「该用户名已被注册」
```

**② 邮箱输入框**（同登录页）

**③ 密码输入框 + 强度指示器**

```
密码输入框:   同登录页，但下方追加强度指示条

强度指示条:
  位置：      密码框下方 8px
  布局：      4 格等宽条 + 右侧强度文字
  高度：      4px，圆角 2px
  颜色变化：
    1/4 格亮（#EF4444）→ 「弱」
    2/4 格亮（#F59E0B）→ 「中」
    3/4 格亮（#3B82F6）→ 「强」
    4/4 格亮（#10B981）→ 「很强」
  非激活格：  #EBF3FF
  判断规则：
    +1：长度 ≥ 8
    +1：包含大写字母
    +1：包含数字
    +1：包含特殊字符（!@#$%^&*等）
  动画：      宽度/颜色变化 300ms ease
```

**④ 确认密码输入框**

```
Label:        「确认密码」
实时校验：    失焦时对比两次密码
匹配：        右侧绿色勾，#10B981
不匹配：      红边框 + 「两次密码不一致」
```

**⑤ 服务协议**

```
布局：        checkbox + 文字行
文字：        「我已阅读并同意 」+「《服务协议》」+「 和 」+「《隐私政策》」
链接颜色：    #3B82F6，点击新标签页打开
未勾选时点击注册按钮：checkbox 抖动（shake 动画 300ms）+ 红色文字「请先阅读并同意协议」
```

**⑥ 注册按钮**：同登录按钮样式，文字「创建账号」

#### 注册表单完整校验规则

| 字段     | 校验规则                                                                            | 错误提示                                      |
| -------- | ----------------------------------------------------------------------------------- | --------------------------------------------- |
| 用户名   | 必填；2-20 位；仅含中英文/数字/下划线；未被占用                                     | 对应提示                                      |
| 邮箱     | 必填；有效 email 格式；未被注册                                                     | 「该邮箱已注册，」+「直接登录？」（蓝色链接） |
| 密码     | 必填；至少 8 位；包含大写字母；包含小写字母；包含数字；包含特殊字符（`!@#$%^&*()`） | 逐条提示（失焦后列出未满足项）                |
| 确认密码 | 必填；与密码一致                                                                    | 「两次输入的密码不一致」                      |
| 协议     | 必须勾选                                                                            | 「请先同意服务协议」                          |

---

### 3.3 忘记密码页 `/forgot-password`

**分为两个步骤（同一页面，内容切换动画 `translateX` 左右滑动）**

#### Step 1：发送验证码

```
标题：        「重置密码」，副标题「输入注册邮箱，我们将发送验证码」
步骤指示器：  两个圆点，当前步骤蓝色实心，下一步空心（简洁，不要步骤条）

邮箱输入框：  同登录页
发送按钮：    「发送验证码」，全宽，48px，蓝色主按钮
              发送后进入倒计时：「重新发送 (59s)」，按钮变次要态（灰色描边，不可点击）
              倒计时数字每秒更新，0 时恢复可点击

返回链接：    「← 返回登录」，13px，#3B82F6，位于按钮下方 16px
```

#### Step 2：验证码 + 新密码

```
标题：        「设置新密码」
副标题：      「验证码已发送至 user@exam**.com（脱敏显示）」

验证码输入框：
  - 6 个独立输入格（每格 48x56px，圆角 8px）
  - 边框 1px #BFDBFE，聚焦 #3B82F6 + 光晕
  - 每格只允许输入 1 位数字，输入后自动跳到下一格
  - 粘贴时自动填入全部格
  - 错误态：全部变红边框 + 抖动动画 + 「验证码错误」

新密码输入框：含强度指示器（同注册页）
确认密码输入框

「重置密码」按钮：全宽，蓝色主按钮

成功后：整页替换为成功状态
  - 绿色大勾（64px，动画从 0 scale 到 1，带弹性曲线 `cubic-bezier(0.34,1.56,0.64,1)`）
  - 「密码已重置」文字
  - 2s 后自动跳转登录页，或点击「立即登录」按钮
```

---

## 四、主界面

### 4.1 整体布局

#### 桌面端（≥ 1024px）— 无 Artifact 面板

```
┌──────────────────────────────────────────────────────────────┐
│  侧边栏 260px          │  主内容区（flex-1）                  │
│  背景：#F0F6FF         │  背景：#F4F8FF                       │
│                        │                                      │
│  ┌──────────────────┐  │  ┌──────────────────────────────┐   │
│  │ Logo + 新建按钮  │  │  │  顶部工具栏（56px）           │   │
│  └──────────────────┘  │  └──────────────────────────────┘   │
│                        │                                      │
│  搜索框（会话搜索）    │           消息区域                    │
│                        │         （flex-1，滚动）              │
│  会话列表              │                                      │
│  （可滚动）            │                                      │
│                        │  ┌──────────────────────────────┐   │
│  ────────────────────  │  │    输入区域（固定底部）        │   │
│  用户区（头像+名字）   │  └──────────────────────────────┘   │
└────────────────────────┴─────────────────────────────────────┘
```

#### 桌面端（≥ 1280px）— 展开 Artifact 面板时

```
┌───────────────────────────────────────────────────────────────────────────────┐
│  侧边栏 260px   │  对话区（flex-1，最小 400px）  │  Artifact 面板（40%，最小 480px）│
│  背景：#F0F6FF  │  背景：#F4F8FF                 │  背景：#FFFFFF                    │
│                 │                                │                                   │
│  Logo + 新建    │  顶部工具栏（56px）            │  ┌─────────────────────────────┐  │
│  搜索框         │  ─────────────────────────     │  │  Artifact 标题栏（48px）     │  │
│  会话列表       │  消息区域（可滚动）             │  │  文件名 + 操作按钮           │  │
│                 │                                │  └─────────────────────────────┘  │
│                 │                                │  Artifact 内容区                   │
│                 │                                │  （代码/文档/图表）                 │
│  ────────────   │  ────────────────────────      │                                   │
│  用户区         │  输入区域（固定底部）           │  ┌─────────────────────────────┐  │
│                 │                                │  │  底部操作栏（复制/下载/运行）│  │
└─────────────────┴────────────────────────────────┴──┴─────────────────────────────┘─┘
```

**Artifact 面板**：当 AI 生成代码、文档、图表等独立内容时自动展开，占右侧约 40% 宽度（见 4.10）。

**侧边栏与内容区之间**：无分割线，仅靠背景色差异（`#F0F6FF` vs `#F4F8FF`）区分——**不使用 border**。

#### 平板端（768px - 1023px）

侧边栏默认折叠，左上角汉堡按钮展开，展开时侧边栏以 overlay 方式叠加（遮罩 `rgba(0,0,0,0.2)`），宽度 280px。Artifact 面板在平板端以 Modal 全屏覆盖（关闭按钮右上角）。

#### 移动端（< 768px）

底部导航 Tab（4个图标：会话列表 / 当前对话 / Artifact / 设置），侧边栏完全替换为全屏对话列表页。Artifact 面板以底部抽屉形式（高度 75vh，顶部拖拽手柄）展示。

---

### 4.2 侧边栏

#### 顶部区域

```
高度：        64px
背景：        #F0F6FF
内容：
  左：        品牌 Logo（24x24 圆形蓝色渐变 + 「Y」字）+ 「元AI」文字（16px/600/#1A2540）
  右：        「新建对话」图标按钮（PenSquare 图标，20px）
              - 背景：透明 → hover: #EBF3FF（8px 圆角）
              - tooltip：「新建对话」（Ctrl+N）
```

#### 会话搜索框

```
距顶部：      8px
外边距：      水平 12px
高度：        36px
背景：        #FFFFFF
边框：        1px solid #BFDBFE，focus 变 #3B82F6
圆角：        8px
左图标：      Search 16px，#9BABC5
Placeholder:  「搜索对话...」
```

#### 会话列表

```
分组标题（日期分组，如「今天」「昨天」「过去 7 天」）：
  - 12px / 500 / #9BABC5
  - 左边距 16px，上下各 8px
  - 无背景、无下划线

会话列表项：
  高度：        44px
  内边距：      水平 8px
  圆角：        8px
  状态：
    默认：      背景透明
    hover：     背景 #EBF3FF（150ms 过渡），显示右侧操作按钮
    active/当前：背景 #EBF3FF + 左侧 3px 蓝色竖线（#3B82F6）

  内容布局：
    左：        对话标题（14px/400/#1A2540，单行超长省略）
    右（hover 显示）：三点图标按钮（MoreHorizontal）

  三点菜单（Dropdown，点击弹出）：
    - 「重命名」（Pencil 图标）
    - 「置顶」/ 「取消置顶」（Pin 图标）
    - 「删除」（Trash2 图标，红色 #EF4444，hover 时菜单项背景 #FEF2F2）

  置顶会话：
    - 排在列表最前（无日期分组）
    - 标题左侧显示小 Pin 图标（12px，#9BABC5）
```

#### 底部用户区

```
高度：        60px
布局：        flex，垂直居中，水平 12px 内边距
内容：
  左：        头像（32x32，圆形）
              - 有头像：显示图片
              - 无头像：蓝色渐变背景 + 用户名首字母（大写白色）
  中：        用户名（14px/500/#1A2540）+ 邮箱（12px/#9BABC5）
  右：        设置图标（Settings 16px，#9BABC5，hover #3B82F6）

点击整个底部区域弹出操作面板（从底部 slide up）：
  - 切换主题（明/暗）
  - 个人设置
  - 退出登录（红色 #EF4444）
```

---

### 4.3 顶部工具栏

```
高度：        56px
背景：        #F4F8FF（与页面底色一致，**无边框**，仅底部有 1px #EBF3FF 分割线）
布局：        左-中-右三区

左区：
  移动端：    汉堡菜单按钮（打开侧边栏）
  桌面端：    当前对话标题（14px/500/#5A6A8A，最大宽度 200px，超长省略，click 可重命名）

中区：        模型选择器（居中）
  按钮样式：  14px/500/#1A2540 + Provider 小 Logo（16x16）+ 模型名 + ChevronDown
  背景：      #FFFFFF，边框 1px #BFDBFE，圆角 8px，高度 36px，内边距 水平 12px
  hover：     边框 #3B82F6，背景 #EFF6FF
  Dropdown：  见「4.4 模型选择器」

右区（按钮间距 4px）：
  联网搜索开关：
    Globe 图标（20px）
    关闭：     #9BABC5，背景透明
    开启：     #3B82F6，背景 #EFF6FF，圆角 8px，外轮廓 2px #BFDBFE
    tooltip：  「联网搜索」（快捷键 Ctrl+Shift+S）
  Artifact 面板切换：
    PanelRight 图标（20px）
    未展开：   #9BABC5，背景透明
    展开中：   #3B82F6，背景 #EFF6FF，圆角 8px
    tooltip：  「展开/收起内容面板」
  分享按钮（Share2 图标，20px，#5A6A8A，tooltip「分享对话」）
  更多操作（MoreHorizontal 图标，20px，#5A6A8A）
    菜单项：   导出 Markdown / 导出 PDF / 清空对话
```

---

### 4.4 模型选择器（Dropdown）

```
触发：        点击顶部工具栏模型选择按钮
位置：        向下展开，与触发按钮左对齐
宽度：        320px
背景：        #FFFFFF
圆角：        10px
阴影：        0 8px 24px rgba(30,64,175,0.12)
内边距：      8px

分组标题：    12px/500/#9BABC5，内边距 8px 水平 12px，无背景
模型列表项：
  高度：      52px
  内边距：    水平 12px
  圆角：      8px
  布局：
    左：      Provider Logo（24x24）
    中：      模型名（14px/500/#1A2540）+ 模型描述（12px/#9BABC5，单行）
    右：      上下文长度标签（12px，背景 #EFF6FF，蓝色文字，圆角 4px）
              + 当前选中时：蓝色勾（Check 14px，#3B82F6）
  hover：     背景 #EFF6FF

分组：
  OpenAI（gpt-4o, gpt-4o-mini）
  Anthropic（claude-3-5-sonnet）
  国内模型（DeepSeek, 通义千问）
```

---

### 4.5 空状态页（新对话/欢迎页）

当没有选中任何会话，或新建会话时展示。

```
布局：        垂直居中，水平居中，最大宽度 680px

──────────────────────────────────────────────────────────────
顶部品牌区（中央，下边距 32px）：
  AI 头像：  72x72，圆形，蓝色渐变背景（from #3B82F6 to #60A5FA）
             白色「Y」Logo，24px bold
             微动效：持续 scale(1→1.04→1) 呼吸动画，3s ease-in-out 循环
  标题：     「你好，我是元AI」，26px/700/#1A2540，上边距 16px
  副标题：   「集成多款顶尖 AI 模型，帮你完成任何任务」，15px/400/#5A6A8A

──────────────────────────────────────────────────────────────
能力展示行（Capability Pills，水平居中，下边距 28px）：
  横排标签，间距 8px，可左右横向滚动（移动端）
  标签样式：
    背景：   #EFF6FF（默认）
    边框：   1px solid #BFDBFE
    圆角：   20px（pill 形）
    内边距： 6px 12px
    字体：   13px/500/#3B82F6
    图标：   14px，左侧，与文字间距 4px
    hover：  背景 #3B82F6，文字 #FFFFFF，border 透明（150ms ease）
  内容（可横向滚动，各展示当前模型能力）：
    🌐 联网搜索
    💻 代码生成与运行
    🎨 图片理解
    📄 文件分析（PDF/Word/Excel）
    🔢 数学推导
    🗣️ 语音对话
    🌍 多语种翻译
  点击标签后：
    在输入框插入对应引导词（如点「代码生成」→ 填入「帮我写一段」）

──────────────────────────────────────────────────────────────
快捷提示卡（Suggestion Cards，2×2 网格，下边距 24px）：
  网格：     gap 12px，响应式（移动端 1列）
  每张卡：
    宽度：   calc(50% - 6px)
    高度：   auto（内容驱动，约 76px）
    背景：   #FFFFFF
    边框：   1px solid #EBF3FF（不要阴影！）
    圆角：   10px
    内边距： 14px
    hover：  边框变 #3B82F6，背景 #EFF6FF，translateY(-2px)（150ms ease）
    active： translateY(0px)，边框 #2563EB
    布局：
      上行：  图标（20px Lucide，#3B82F6）+ 标题（14px/600/#1A2540）
      下行：  描述文字（12px/400/#5A6A8A，单行超长省略）
    出现动画：卡片依次延迟 60ms 从下方 fade-in（index × 60ms）

  示例卡（每次随机从下方列表取 4 张轮换）：
    ✨ Sparkles  「创意写作」  → 「帮我写一个关于时间旅行的科幻短篇」
    Bug          「代码调试」  → 「帮我排查这段代码为什么报 TypeError」
    Globe        「联网搜索」  → 「搜索今天最新的 AI 行业动态」
    FileText     「文档总结」  → 「帮我总结这份 PDF 文件的要点」
    Languages    「语言翻译」  → 「把下面内容翻译成地道英文」
    BarChart2    「数据分析」  → 「解释一下这份数据的趋势和异常」
    Lightbulb    「头脑风暴」  → 「帮我为新产品发布想 10 个营销创意」
    GraduationCap「学习辅导」  → 「用简单的方式解释量子纠缠是什么」

  点击卡片：
    - 标题文字填入输入框（不直接发送）
    - 输入框自动聚焦，光标定位到末尾
    - 用户可继续编辑后发送

──────────────────────────────────────────────────────────────
底部：        输入框（与对话页相同，始终可见）
              输入框下方小字提示：「元AI 可能犯错，请核实重要信息」
              13px/#9BABC5，居中
```

---

### 4.6 对话页（聊天界面）

#### 消息区域

```
最大宽度：    800px（居中，Artifact 面板展开时自动收窄至 560px）
内边距：      水平 24px，顶部 24px，底部 16px

消息间距：
  同角色消息：12px
  换角色消息：24px
```

**用户消息**

```
对齐：        右对齐
气泡背景：    linear-gradient(135deg, #3B82F6, #60A5FA)
文字颜色：    #FFFFFF，15px/400，行高 24px
圆角：        14px 14px 4px 14px（右下尖角）
内边距：      12px 16px
最大宽度：    75%

附件展示（气泡内，内容上方）：
  图片：      最大高度 200px，圆角 8px，object-contain，点击放大预览
  文档文件：  宽度充满气泡，高度 48px，背景 rgba(255,255,255,0.15)
              圆角 8px，FileText 图标（白色）+ 文件名（白色，13px）
  音频文件：  波形图横条 + 播放按钮（白色）+ 时长

消息操作（消息 hover 时出现，右侧）：
  编辑按钮（Pencil 图标）：
    点击后：气泡变为带取消/保存的编辑态输入框
    编辑态：
      背景从气泡蓝变为 #FFFFFF，边框 1px solid #3B82F6
      右下角：「取消」ghost 按钮 + 「保存并重新生成」蓝色按钮
      保存后：触发从该消息重新生成（覆盖后续对话分支）

出现动画：    opacity 0→1 + translateY(8px→0)，250ms ease-out
```

**AI 消息**

```
对齐：        左对齐
气泡：        无（透明背景，不要气泡边框）
AI 头像：     32x32，圆形，蓝色渐变，紧贴消息左侧，上对齐

深度推理展示（Thinking 展示区，在正式回答前）：
  触发条件：  当所选模型支持 thinking 模式时（如 claude-3-7-sonnet-thinking、o1 系列）
  样式：
    背景：    #F4F8FF，圆角 8px，内边距 12px 14px
    左侧竖线：2px solid #BFDBFE
    标题行：  Brain 图标（14px，#5A6A8A）+ 「推理过程」（13px/500/#5A6A8A）+ 折叠箭头
    内容：    推理文字，13px/400/#5A6A8A，行高 22px
    默认折叠：仅展示 2 行 + 「展开全部」链接
    展开状态：全文 + 「收起」链接（300ms 高度动画）
    状态指示：思考中时标题前显示旋转图标（Loader 14px，#5A6A8A）

内容渲染：    Markdown（见下方规范）
最大宽度：    100%（受父容器限制）

联网引用展示（消息内，内容末尾前）：
  触发条件：  联网搜索模式下 AI 回答带来源
  样式：
    分割线上方标题：Globe 图标（12px，#5A6A8A）+ 「参考来源」（12px/500/#5A6A8A）
    来源条目：  横向滚动行，每条：
      外观：    圆角 6px，背景 #EFF6FF，边框 1px #BFDBFE，内边距 6px 10px
      内容：    网站 favicon（14x14）+ 标题（12px/400/#1A2540，最多 20 字）
      hover：   边框 #3B82F6，文字 #3B82F6，cursor pointer
      点击：    新标签打开原文链接

流式输出时：
  - 文字逐字/逐词出现（无额外动画，直接追加）
  - 末尾显示蓝色光标（2px 宽，16px 高，#3B82F6，0.6s 闪烁）
  - 光标在流结束后消失（opacity 0，200ms）

操作按钮（消息 hover 时出现，消息下方）：
  按钮列表：  [复制] [重新生成] [点赞] [点踩] [在 Artifact 中打开]（仅代码块时）
  样式：      图标 14px + 文字 12px，背景透明，hover #EBF3FF，圆角 6px，高度 28px
  间距：      4px
  重新生成下拉：点击右侧小箭头 → 「使用当前模型重新生成」/「切换模型重新生成」

对话分支指示器（多次重新生成后）：
  位置：      AI 消息右上角
  样式：      「1/3」样式翻页器（ChevronLeft + 「2/3」文字 + ChevronRight）
              12px/#5A6A8A，按钮 20x20px，hover #EBF3FF 圆角 4px
  说明：      记录对同一用户消息的多次生成结果，可前后切换查看
```

**推荐追问（Suggested Follow-ups）**

AI 完成回答后，在操作按钮行下方展示 2-3 个追问建议：

```
出现时机：    流式输出完毕后 300ms 延迟显示
布局：        横向排列（溢出隐藏），间距 8px
每个标签：
  样式：      border 1px solid #BFDBFE，圆角 16px，内边距 6px 14px
              背景 #FFFFFF，文字 13px/400/#5A6A8A
  hover：     边框 #3B82F6，文字 #3B82F6，背景 #EFF6FF（150ms ease）
  最大宽度：  220px，超长省略
  点击：      文字填入输入框（不直接发送），输入框聚焦
内容生成：    由后端根据上下文动态生成（非前端写死）
不显示条件：  用户正在输入时，隐藏（防干扰）
```

**Markdown 渲染样式规范**

| 元素         | 样式                                                                        |
| ------------ | --------------------------------------------------------------------------- |
| `h1/h2/h3`   | 700 / 20px/17px/15px / `#1A2540` / 上方 20px 间距                           |
| `p`          | 15px / 400 / `#1A2540` / 行高 26px / 段落间距 12px                          |
| `strong`     | 600 / 颜色不变                                                              |
| `em`         | 斜体 / 颜色不变                                                             |
| `a`          | `#3B82F6`，hover 下划线                                                     |
| `ul/ol`      | 左缩进 20px，li 间距 6px                                                    |
| `blockquote` | 左 3px 蓝色边（#3B82F6）/ 背景 `#EFF6FF` / 内边距 12px 16px / 圆角右侧 4px  |
| `table`      | 无 border，header 背景 `#EFF6FF`，行间隔颜色 `#F4F8FF`，td 内边距 10px 14px |
| `hr`         | 1px / `#EBF3FF` / 上下各 16px                                               |
| 行内 `code`  | 背景 `#EFF6FF`，`#1A2540` 文字，等宽字体，圆角 4px，内边距 2px 6px          |

**代码块样式（含 Artifact 入口）**

```
容器圆角：    8px
边框：        1px solid #EBF3FF（不要阴影）
背景：        #F8FAFF（亮色）/ #1E2A3B（深色主题）

顶部 Header（36px）：
  背景：      #EFF6FF（亮色）/ #16243A（深色主题）
  左侧：      语言标签（12px/500/#5A6A8A）
  右侧（从右到左）：
    「在面板中查看」按钮（PanelRight 图标 14px + 文字，点击打开 Artifact 面板）
    「复制」按钮（Copy 图标 14px）
    点击复制后：图标变 Check，2s 后恢复，颜色：#5A6A8A → hover #3B82F6

代码区：      内边距 16px，overflow-x auto，等宽字体 13px，最大高度 400px（超出显示滚动）
语法高亮：    亮色使用 github-light，深色使用 github-dark
行号：        可选（默认不显示）
```

#### 输入区域

```
位置：        页面固定底部
背景：        #F4F8FF（与页面底色一致），顶部 1px #EBF3FF 线条（淡，不突出）
内边距：      水平 24px，顶部 12px，底部 16px（+ safe-area-inset-bottom）
最大宽度：    800px，居中（与消息区对齐）

输入框容器：
  背景：      #FFFFFF
  边框：      1px solid #BFDBFE
  圆角：      14px
  focus 态：  边框 #3B82F6 + 光晕 3px rgba(59,130,246,0.15)
  过渡：      border-color 150ms ease，box-shadow 150ms ease

──── 内部结构（从上到下）────────────────────────────────────

① 文件附件预览行（有附件时显示，无则隐藏）：
  布局：      横向可滚动行，内边距 12px 12px 0
  图片预览：  64x64，圆角 6px，object-cover
              右上角 X 删除按钮（16x16，背景 rgba(26,37,64,0.6)，圆角 50%）
  文档预览：  168x48，圆角 6px，背景 #EFF6FF，内边距 8px 10px
              左侧：FileText/FileSpreadsheet 图标（20px，#3B82F6）
              右侧：文件名（13px/#1A2540，1行省略）+ 大小（11px/#9BABC5）
              右上角 X 按钮（同图片）
  音频预览：  240x48，圆角 6px，背景 #EFF6FF
              Play/Pause 图标（20px）+ 音频名 + 时长（11px/#9BABC5）
  上传进度：  文件预览上覆盖半透明蒙层 + 进度圆环（白色，24px）+ 百分比

② 文字输入区：
  类型：      textarea（自动高度）
  最小高度：  52px（单行）
  最大高度：  200px（超出滚动）
  内边距：    14px 16px
  字体：      15px / #1A2540
  背景：      透明
  边框：      无
  resize：    none
  Enter 行为：发送（桌面端），Shift+Enter 换行
  移动端：    Enter 换行，发送按钮触发发送

③ 底部工具栏行（高度 44px，内边距 0 8px 8px）：
  左侧工具组（间距 2px）：
    附件上传（+）：
      图标：   Paperclip，20px，#9BABC5 → hover #3B82F6（150ms）
      点击：   展开 Dropdown（上方弹出）
        - 📎 上传文件（支持 PDF/Word/Excel/TXT，最大 50MB）
        - 🖼️ 上传图片（支持 JPG/PNG/WebP，最大 20MB，最多 5 张）
        - 🎵 上传音频（支持 MP3/M4A/WAV，最大 25MB）

    语音输入（Mic）：
      图标：   Mic，20px，#9BABC5 → hover #3B82F6（150ms）
      激活态：见 4.8 语音输入界面

    联网搜索开关（Globe）：
      默认关：  Globe，20px，#9BABC5
      开启：    Globe，20px，#3B82F6，背景 #EFF6FF，圆角 6px，带小蓝点指示
      tooltip： 「联网搜索开关（Ctrl+Shift+S）」

  分隔线：     1px solid #EBF3FF，高度 16px，左右边距 4px

  右侧（靠右对齐，间距 4px）：
    当前模型小标签：
      文字：   12px/500/#3B82F6，背景 #EFF6FF，圆角 4px，内边距 3px 8px
      内容：   Provider 图标（12px）+ 模型短名（如「GPT-4o」）
      点击：   打开模型选择器
    字符计数：
      文字：   12px/#9BABC5
      内容：   「120 / 4000」
      超限：   变红 #EF4444
    发送/停止按钮（40x40px，圆角 10px）：
      空输入态（无文字且无文件）：
        背景：   #EBF3FF，图标 SendHorizontal 20px/#9BABC5，disabled
      有内容态：
        背景：   #3B82F6，图标 SendHorizontal 20px/#FFFFFF
        hover：  背景 #2563EB，scale(1.04)（100ms ease）
        active： scale(0.95)（80ms ease）
      AI 生成中：
        按钮切换为「停止生成」（Square 图标，16px/#FFFFFF）
        背景：   #EF4444（红色，提示危险操作）
        hover：  #DC2626
        点击：   立即停止 SSE 流，保留已生成内容
        切换动画：scale(0.8→1) + opacity(0→1)，180ms ease-out
```

---

### 4.7 加载/过渡状态

#### AI 思考中（等待第一个 token）

```
位置：        消息列表底部，AI 头像旁
显示：        3 个跳跃小圆点（6px，#3B82F6）
动画：        依次 scale 0.6→1→0.6，每个延迟 0.15s，循环，easing: ease-in-out
```

#### 消息加载中（历史消息拉取）

```
显示：        骨架屏（Skeleton）
样式：        圆角 4px，背景从 #EBF3FF → #F4F8FF 渐变扫过（shimmer 动画，1.5s 循环）
不要用 spinner，骨架屏更自然
```

#### 消息列表滚动到底部

```
触发：        新消息出现时（AI 流式输出中），若用户在底部 100px 范围内则自动滚到底
用户向上翻：  停止自动滚动，右下角出现「↓ 回到底部」悬浮按钮
  样式：      圆形 40px，背景 #1A2540/80%，图标 ArrowDown 18px 白色，blur 背景效果
  点击：      平滑滚动到底，按钮消失（opacity 0 + scale 0.8，180ms）
```

---

### 4.8 语音输入界面

点击输入框工具栏的 Mic 图标后，进入语音输入模式。

#### 录音激活态（覆盖输入区）

```
布局：        替换输入框区域显示语音 UI（不覆盖全屏）
高度：        120px
背景：        #FFFFFF，圆角 14px，边框 1px solid #EF4444（红色，表示录音中）
              外发光：0 0 0 4px rgba(239,68,68,0.15)

中央波形区：
  宽度：      充满容器，内边距 水平 24px
  波形：      20 根竖线（每根 3px 宽，圆角 2px），间距 4px
  颜色：      录音中 #3B82F6，静音 #BFDBFE
  动画：      各竖线高度由麦克风音量实时驱动（8px~48px 之间），无缓动
  静音检测：  连续 2s 无声音 → 波形变灰 + 显示「未检测到声音，请靠近麦克风」

底部控制行（水平居中，间距 24px）：
  左侧「取消」按钮：
    图标：    X，20px，#5A6A8A
    文字：    「取消」，13px/#5A6A8A
    点击：    关闭语音输入，恢复原始输入框内容
  中间录音状态指示：
    录音中：  红色实心圆点（10px）闪烁（1s 循环）+ 录音时长 「00:15」（16px/600/#1A2540）
    最长录音：180s（3分钟），达到后自动停止
  右侧「完成」按钮：
    图标：    Check，20px
    文字：    「完成」，13px
    背景：    #3B82F6，文字 #FFFFFF，圆角 8px，内边距 8px 16px
    点击：    停止录音 → 转文字（见下方）

实时转写条（波形区上方，有识别结果时出现）：
  文字：      13px/400/#5A6A8A，斜体
  内容：      实时识别结果（WebSpeech API 或后端流式识别），文字淡入
  确认后：    转写文本填入输入框，可继续编辑
```

#### 语音识别中状态

```
转写加载：    波形区替换为「识别中...」文字 + 旋转 Loader 图标（16px，#3B82F6）
识别完成：    结果文字填入输入框，输入框自动聚焦，光标在末尾
              语音 UI 收起（height 0，200ms ease-in）
识别失败：    Toast 提示「语音识别失败，请重试」（#EF4444 背景），返回输入模式
```

#### 移动端长按激活

```
移动端：      长按发送按钮区域 → 进入语音录音
              实时显示：发送按钮变红色麦克风 + 上方波形条（紧凑型，高度 56px）
              松手：停止录音 → 识别 → 发送
              上滑：取消录音（参考微信）
```

---

### 4.9 Agent 工具调用展示

当 AI 使用工具（联网搜索、代码执行、文件读取等）时，在 AI 消息头像下方、正式回答前展示工具调用过程。

#### 工具调用卡（ToolCallCard）

```
位置：        AI 头像下方，正式回答上方
布局：        垂直堆叠，每个工具调用一张卡

单张卡样式：
  背景：      #F4F8FF
  边框：      1px solid #EBF3FF
  圆角：      8px
  内边距：    10px 14px
  宽度：      充满消息宽度

  标题行（可点击展开/折叠）：
    左侧：    工具图标（16px）+ 工具名（13px/500/#5A6A8A）
    中间：    操作描述（13px/400/#1A2540，如「搜索 "OpenAI GPT-5 发布时间"」）
    右侧：    状态图标（16px）+ 用时（11px/#9BABC5，如「1.2s」）+ 折叠箭头

  状态图标：
    进行中：  Loader 旋转图标，#3B82F6
    成功：    CheckCircle2 图标，#10B981（出现动画：scale 0→1，200ms 弹性曲线）
    失败：    XCircle 图标，#EF4444
    等待：    Clock 图标，#9BABC5

  展开内容（默认折叠）：
    上下 padding 8px，分割线 1px #EBF3FF
    内容类型由工具决定（见下方各工具详情）

工具调用组折叠（多个工具时）：
  收起：      仅显示「已完成 N 个工具调用」摘要行（摘要样式同卡片标题）
  展开：      全部卡片从上到下堆叠展示（展开/收起动画 220ms）
```

#### 各工具展示详情

**① 联网搜索**

```
图标：        Globe（#3B82F6）
操作描述：    「搜索 "关键词"」

展开内容：
  搜索结果列表（最多显示 5 条）：
    每条：    序号 + 标题（13px/500/#1A2540）+ 来源域名（11px/#9BABC5）
    hover：   文字 #3B82F6，cursor pointer，click 新标签打开
  底部：      「共找到 X 条结果」（11px/#9BABC5）
```

**② 代码执行（Code Interpreter）**

```
图标：        Terminal（#5A6A8A）
操作描述：    「执行 Python 代码」

展开内容：
  代码块（紧凑型，无 Header，最大高度 120px，overflow scroll）
    背景：    #1E2A3B，文字 #CDD6F4，等宽字体 12px
  执行输出区：
    背景：    #F0F6FF，内边距 8px 12px，圆角 4px，字体 13px 等宽
    成功输出：#1A2540
    错误输出：#EF4444
  图表/图片输出：
    最大高度 240px，圆角 6px，居中显示
    点击可在 Artifact 面板中查看全屏
```

**③ 文件读取/分析**

```
图标：        FileSearch（#5A6A8A）
操作描述：    「分析文件 "xxx.pdf"（共 42 页）」

展开内容：
  文件信息行：FileText 图标 + 文件名 + 大小（13px/#5A6A8A）
  分析摘要：  13px/400/#5A6A8A，最多 2 行
```

**④ 图像生成**

```
图标：        ImagePlus（#3B82F6）
操作描述：    「生成图片」

进行中：      生成进度动画（渐变扫光骨架屏，240px 高度）
完成后：
  图片展示：  充满宽度，最大高度 320px，圆角 8px
  操作行（图片下方）：下载按钮 + 「查看原图」 + 「重新生成」
  图片说明：  13px/#5A6A8A（Prompt 摘要）
```

---

### 4.10 Artifact 侧边面板

展示 AI 生成的可独立查看/编辑的内容（代码文件、Markdown 文档、HTML 页面、图表等）。

#### 面板结构

```
宽度：        40%（大桌面 ≥ 1280px），固定 480px（中等桌面 1024-1279px）
背景：        #FFFFFF
左侧边框：    1px solid #EBF3FF（与对话区分隔，不用阴影）
展开动画：    translateX(100%→0)，300ms cubic-bezier(0.4,0,0.2,1)
关闭动画：    translateX(0→100%)，250ms ease-in

──── 标题栏（48px）──────────────────────────────────────────
  背景：      #F4F8FF
  左侧：      文件类型图标（20px，#5A6A8A）+ 文件名/标题（14px/600/#1A2540）
  右侧（按钮间距 4px）：
    复制全部（Copy 图标，20px，#5A6A8A）
    下载（Download 图标，20px，#5A6A8A，下载为对应格式文件）
    弹出全屏（Maximize2 图标，20px，#5A6A8A，Modal 全屏查看）
    关闭（X 图标，20px，#5A6A8A）

──── 类型选项卡（Type Tabs，当 Artifact 有多个可切换视图时）──
  代码 Artifact：
    [代码] [预览]（仅 HTML/SVG/React 等可预览类型显示）
  文档 Artifact：
    [文档] [源码]
  样式：       Tab 条（32px 高），下划线激活态（2px #3B82F6），文字 13px/500

──── 内容区（flex-1，overflow auto）──────────────────────────
  代码文件：
    语法高亮代码编辑器（只读 or 可编辑，取决于功能开关）
    背景：    #F8FAFF（亮色）/ #1E2A3B（深色）
    字体：    13px 等宽，行高 22px
    行号：    显示，36px 宽，#9BABC5
    当前编辑行高亮：背景 rgba(59,130,246,0.06)

  Markdown 文档：
    正常 Markdown 渲染（同消息内规范）
    内边距：  24px

  HTML 预览：
    iframe 嵌入（sandbox 安全限制），宽度 100%，高度 100%

  图表（Chart）：
    渲染为交互式图表组件（ECharts / recharts）
    工具提示、图例、缩放均可交互

──── 底部状态栏（36px）──────────────────────────────────────
  背景：      #F4F8FF
  左侧：      文件类型说明（12px/#9BABC5，如「TypeScript · 142 行」）
  右侧：      「在新标签中运行」/ 「在 CodeSandbox 打开」（可选，仅 JS/HTML 类型）
```

#### Artifact 面板触发时机

```
自动展开：    AI 回答中生成超过 30 行的代码块时（自动识别）
手动触发：    点击代码块 Header 中「在面板中查看」按钮
             点击顶部工具栏「Artifact 面板」图标
多个 Artifact：
  标题栏左侧显示「< 1/3 >」翻页控件（同对话分支指示器样式）
  切换时内容区 fade 过渡（200ms）
```

---

### 4.11 移动端专属设计

移动端（< 768px）在上述基础上的差异设计。

#### 底部输入区域（移动端）

```
输入框容器贴底，安全区域内边距由 safe-area-inset-bottom 计算
工具栏图标仅显示最关键 3 个（附件、语音、发送），其余收入「+」扩展按钮
发送按钮：    40x40px，右侧内嵌在输入框中

发送中：      底部出现进度条（宽度随生成字数变化，背景 #3B82F6，高度 2px）
```

#### 移动端手势

```
消息列表：
  下拉刷新：  加载更早的历史消息，顶部出现 Loader（#3B82F6）
  长按消息：  出现浮动操作菜单（复制/重新生成/点赞/点踩）
              样式：黑色背景（#1A2540），圆角 10px，白色文字/图标
              出现动画：scale(0.8→1) + opacity，180ms 弹性曲线
AI 消息气泡区：
  左滑：      显示「重新生成」按钮（蓝色，从右侧滑出）
用户消息气泡：
  左滑：      显示「编辑」按钮（灰色，从右侧滑出）
侧边栏：
  屏幕左侧边缘右滑：打开侧边栏（全屏抽屉，遮罩 rgba(0,0,0,0.4)）
  侧边栏区域左滑：  关闭侧边栏
```

---

## 五、设置页面与个人中心

设置页 `/settings` 和个人中心 `/profile` 共享同一套容器布局，以 **全屏 Modal 覆盖** 方式展示在主界面之上（路由切换，URL 会变化，但视觉上保留背景对话页的虚化感）。

### 5.0 共用容器布局

#### 桌面端（≥ 1024px）

```
┌─────────────────────────────────────────────────────────────────────┐
│  背景：主界面虚化（blur 8px + rgba(15,24,41,0.45) 蒙层）           │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  Modal 容器                                                  │   │
│  │  宽度：900px（max-width）   高度：90vh（max-height）          │   │
│  │  背景：#F4F8FF              圆角：12px                        │   │
│  │  阴影：0 24px 64px rgba(30,64,175,0.18)                      │   │
│  │                                                              │   │
│  │  ┌──────────────┬────────────────────────────────────────┐   │   │
│  │  │ 左侧导航     │  右侧内容区                            │   │   │
│  │  │ 220px        │  flex-1，内边距 32px，overflow-y auto  │   │   │
│  │  │ 背景：#F0F6FF│  背景：#F4F8FF                         │   │   │
│  │  │              │                                        │   │   │
│  │  │  导航列表    │  页面标题（22px/700/#1A2540）          │   │   │
│  │  │              │  副标题（14px/400/#5A6A8A）            │   │   │
│  │  │              │  ──────────────────────────           │   │   │
│  │  │              │  各设置区块（见各节详情）              │   │   │
│  │  └──────────────┴────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

**容器动画**：`scale(0.96→1) + opacity(0→1)`，250ms `cubic-bezier(0.4,0,0.2,1)`
**关闭方式**：右上角 X 按钮 / ESC 键 / 点击蒙层区域

#### 移动端（< 768px）

全屏页面覆盖（无蒙层），使用系统路由跳转。

```
顶部导航栏（56px）：
  左：ChevronLeft 返回按钮（20px）+ 「返回」文字（14px/#3B82F6）
  中：页面标题（16px/600/#1A2540）
背景：#F4F8FF
内容：单列展示，无左侧导航
```

---

### 5.1 左侧导航（桌面端共用）

```
顶部区域（64px）：
  页面大标题：「账号管理」，16px/600/#1A2540，内边距 左16px
  X 关闭按钮（右上角，20px，#9BABC5 → hover #1A2540）

导航分组（中部，padding 8px）：
  分组标题：  12px/500/#9BABC5，左边距 8px，上下各 6px

  ── 个人中心 ──
  [User]     个人资料
  [Shield]   账号安全

  ── 系统偏好 ──
  [Palette]  外观与显示
  [Bot]      AI 偏好
  [Mic]      语音设置
  [Keyboard] 快捷键

  ── 数据 ──
  [Database] 数据与存储
  [HelpCircle] 关于元AI

导航项样式：
  高度：      40px
  内边距：    水平 8px
  圆角：      8px
  图标：      16px，左侧，与文字间距 8px
  文字：      14px/400/#5A6A8A
  hover：     背景 #EBF3FF，文字 #1A2540（150ms ease）
  active：    背景 #EBF3FF，左侧 3px 蓝色竖线，文字 #3B82F6，图标 #3B82F6

底部（绝对定位，底部 0）：
  版本号：    「元AI v1.0.0」，12px/#9BABC5，居中，内边距 底部 16px
```

---

### 5.2 个人资料页（`/profile`）

#### 布局

```
页面标题：    「个人资料」
副标题：      「管理你的基本信息和公开展示内容」
```

#### 头像区域

```
布局：        水平排列，左头像右信息，距顶部 0
头像展示：
  尺寸：      80x80px，圆形
  有头像：    显示用户上传的图片
  无头像：    蓝色渐变 + 首字母（28px/700/white）
  右下角：    相机图标浮层（24x24，背景 #1A2540/70%，圆形，Camera 图标 12px 白色）
              hover 时整个头像区出现蒙层 + 「更换」文字（12px/white）

头像右侧信息（垂直排列）：
  用户名：    20px/700/#1A2540
  邮箱：      13px/#5A6A8A
  会员标签：  「免费版」或「Pro」
    免费：    背景 #EFF6FF，蓝色文字，圆角 4px，内边距 2px 8px，12px/500
    Pro：     背景 linear-gradient(135deg,#3B82F6,#60A5FA)，白色文字，12px/500

头像操作按钮（头像下方，间距 8px）：
  「上传头像」ghost 按钮（Upload 图标）
  「删除头像」ghost 按钮（Trash2 图标，#EF4444，仅有头像时显示）
  支持格式提示：「支持 JPG/PNG/WebP，最大 5MB」，12px/#9BABC5
```

#### 表单区块（设置区通用「区块」样式）

```
区块容器：
  背景：      #FFFFFF
  圆角：      10px
  内边距：    0（内部各行自带间距）
  上下边距：  16px
  不使用 border 或阴影

区块标题行：
  高度：      52px
  内边距：    水平 20px
  字体：      15px/600/#1A2540
  下方：      1px solid #EBF3FF 分割线

表单行：
  高度：      56px（单行内容）/ auto（多行）
  内边距：    水平 20px
  布局：      flex，左Label 右Value/输入框，垂直居中
  分割线：    行间 1px solid #EBF3FF（最后一行无）
  Label：     14px/400/#5A6A8A，最小宽度 100px
  Value/输入框：flex-1，右对齐（只读时）/ 全宽（编辑时）
```

#### 基本信息区块

```
区块标题：「基本信息」

用户名行：
  Label：    「用户名」
  默认态：   右侧显示当前用户名（14px/400/#1A2540）+ 「编辑」图标按钮（Pencil 16px，#9BABC5）
  编辑态：
    整行切换为 Input + 保存/取消（行内，不弹 Modal）
    输入框：  border 1px solid #3B82F6，圆角 8px，高度 36px
    右侧：    「取消」ghost 14px + 「保存」蓝色小按钮 14px
    实时校验：2-20 位，用户名唯一（debounce 500ms 检测）
    校验状态：右侧绿勾/红 X（同注册页规范）

个性签名行：
  Label：    「个性签名」
  默认态：   右侧当前签名（14px/#5A6A8A）或「未填写」（14px/#9BABC5）+ 编辑图标
  编辑态：   textarea，最大 100 字，右下角字数「xx/100」
  说明文字：  「签名将显示在你的公开对话分享中」，12px/#9BABC5，行底部

注册时间行：
  Label：    「注册时间」
  值：       「2025 年 6 月 27 日」，14px/400/#1A2540（只读，无编辑图标）
```

#### 使用数据区块

```
区块标题：「本月使用情况」（副标题：「每月 1 日重置」，13px/#9BABC5）

数据行（3 项，水平排列，各占 1/3）：
  每项布局：垂直居中，左图标（20px，#3B82F6）右数值
  ① 对话次数：MessageSquare 图标，「128 次对话」
  ② 消耗 Token：Zap 图标，「42.3k Token」
  ③ 上传文件：Paperclip 图标，「17 个文件」
  数值字体：18px/700/#1A2540
  说明字体：12px/#9BABC5

进度条（仅免费用户显示）：
  标题：「本月免费额度」，13px/500/#1A2540 + 「升级 Pro 解锁无限制」蓝色链接
  进度条：宽度 100%，高度 6px，圆角 3px
    已用背景：#3B82F6，剩余背景：#EBF3FF
  文字：「已用 42,312 / 100,000 Token」，12px/#5A6A8A，右对齐
```

---

### 5.3 账号安全页（`/profile/security`）

```
页面标题：「账号安全」
副标题：  「管理你的登录凭证和账号保护设置」
```

#### 邮箱区块

```
区块标题：「登录邮箱」

邮箱行：
  Label：    「当前邮箱」
  值：       当前邮箱（脱敏，如 li**@gmail.com），14px/#1A2540
  右侧：     「更换邮箱」蓝色文字按钮（13px/500/#3B82F6）
  点击「更换邮箱」→ 弹出 Modal：
    标题：「更换邮箱」
    Step 1：输入新邮箱 → 发送验证码（倒计时 60s，同注册页）
    Step 2：输入邮箱验证码（6 位数字格）→ 确认更换
    成功：Toast「邮箱已更新」+ Modal 关闭
```

#### 密码区块

```
区块标题：「登录密码」

密码行：
  Label：    「当前密码」
  值：       「••••••••」，14px/#9BABC5
  右侧：     「修改密码」蓝色文字按钮
  点击 → 弹出 Modal（竖向三步）：
    标题：「修改密码」
    当前密码输入框（Lock 图标，右侧眼睛切换）
    新密码输入框（含强度条，同注册页规范）
    确认新密码输入框
    「保存修改」蓝色按钮 + 「取消」ghost 按钮
    成功：Toast「密码已更新，请重新登录」→ 2s 后退出登录
```

#### 关联账号区块

```
区块标题：「关联登录方式」
副标题：  「通过第三方账号快速登录元AI」

关联项（每项一行，高度 60px）：
  列表：     微信 / Google / GitHub

  每行布局：
    左：     Provider Logo（24x24）+ Provider 名称（15px/500/#1A2540）
    右：     状态 + 操作按钮

  已关联态：
    状态标签：「已关联」（12px，#10B981，背景 #ECFDF5，圆角 4px，内边距 2px 8px）
    操作：    「解除关联」文字按钮（13px/#EF4444）
    解除确认：弹 Modal「确认解除微信关联？解除后无法通过微信登录，请确保已设置密码」
              取消 / 解除（红色按钮）

  未关联态：
    状态标签：「未关联」（12px，#9BABC5）
    操作：    「立即关联」蓝色文字按钮（13px/#3B82F6）
    点击：    弹出 OAuth 授权窗口

  微信关联说明（微信行底部，仅移动端显示）：
    「微信关联需在微信内打开，或使用微信扫码」，12px/#9BABC5
```

#### 危险操作区块

```
区块标题：「危险操作」
区块样式：区块 border 变为 1px solid rgba(239,68,68,0.2)（淡红色边框，与其他区块区分）

注销账号行：
  Label：    「注销账号」
  描述：     「永久删除账号及所有对话数据，不可恢复」，13px/#9BABC5
  右侧：     「注销账号」按钮（描边样式，border #EF4444，文字 #EF4444）
  点击：     二次确认 Modal
    标题：「注销账号」（标题颜色 #EF4444）
    正文：「此操作将永久删除你的账号和所有对话记录，且不可恢复。是否确认注销？」
    输入框：  「请输入 "删除账号" 以确认」（防误触）
    按钮：   「取消」ghost + 「确认注销」红色按钮（输入正确后才可点击）
```

---

### 5.4 外观与显示（`/settings/appearance`）

```
页面标题：「外观与显示」
```

#### 主题区块

```
区块标题：「主题」

主题选择（三个选项卡，水平排列，间距 12px）：
  每个卡：   宽度 calc(33.3% - 8px)，高度 80px，圆角 10px，cursor pointer
  卡内容：
    上 2/3：  主题预览小缩略图（亮色/暗色/蓝色渐变装饰，用 CSS 绘制）
    下 1/3：  主题名（13px/500）+ 选中时蓝色勾（CheckCircle 14px，#3B82F6）
  未选中：   border 1px solid #EBF3FF，背景 #FFFFFF
  选中：     border 2px solid #3B82F6，背景 #EFF6FF
  选项：
    「跟随系统」：上半部分左亮右暗（对角分割）
    「浅色模式」：纯白 + 淡蓝背景小方块
    「深色模式」：深蓝黑 + 蓝色光点装饰

切换动画：    全局颜色变量 200ms ease 过渡（同动画规范）
```

#### 字体大小区块

```
区块标题：「字体大小」
副标题：  「影响对话内容的文字大小」

滑动条：
  轨道：      高度 4px，圆角 2px，背景 #EBF3FF
  已选范围：  #3B82F6
  滑块：      16x16px，圆形，白色 + border 2px #3B82F6，hover shadow
  刻度：      「小 A」—「A」—「大 A」（三点标注，12px/#9BABC5）
  取值：      small(13px) / medium(15px，默认) / large(17px)
  实时预览：  右侧展示「预览文字：今天帮我写一段关于...」随滑动实时缩放（200ms ease）
```

#### 消息密度区块

```
区块标题：「消息密度」
副标题：  「控制消息之间的间距」

单选组（水平排列，3 项）：
  选项：     紧凑 / 标准（默认）/ 宽松
  选中态：   蓝色背景 #EFF6FF，蓝色边框 #3B82F6，文字 #3B82F6
  未选中：   白色背景，灰色边框
  高度：     36px，圆角 8px，内边距 水平 20px，文字 14px/500
```

#### 语言区块

```
区块标题：「语言」

语言选择行：
  Label：    「界面语言」
  右侧：     Dropdown 选择器（同模型选择器样式，宽度 180px）
             选项：简体中文（默认）/ 繁體中文 / English / 日本語
  选择后：   Toast「语言已切换，重新加载后生效」
             「立即重载」蓝色链接
```

---

### 5.5 AI 偏好（`/settings/ai`）

```
页面标题：「AI 偏好」
副标题：  「自定义 AI 的行为和回复风格」
```

#### 默认模型区块

```
区块标题：「默认模型」

模型选择行：
  Label：    「新对话默认使用」
  右侧：     模型选择器（同主界面顶部栏 Dropdown，宽度 220px）
             选中后：更新本地偏好，新建对话自动使用该模型
```

#### 回复风格区块

```
区块标题：「回复风格」
副标题：  「影响 AI 回复的详细程度和创意度」

滑动条（创意度）：
  Label：   「创意度（Temperature）」
  左端：    「精确」（0.0）
  右端：    「创意」（2.0）
  默认：    1.0（中间）
  刻度：    0 / 0.5 / 1.0 / 1.5 / 2.0 五个刻度点
  当前值：  滑块上方浮动气泡显示数值（12px/#FFFFFF，背景 #3B82F6）
  说明：    「数值越高，回复越有创意但可能不够准确」，12px/#9BABC5

回复长度单选：
  Label：   「回复长度偏好」
  选项：    「简洁」/ 「适中（默认）」/ 「详细」
  样式：    同消息密度单选组
```

#### 自定义系统提示词区块

```
区块标题：「自定义系统提示词」
副标题：  「添加一段固定的背景信息，在每次对话开始时自动发送给 AI」

文本域：
  高度：    120px（固定）
  字体：    14px/400/#1A2540，行高 22px
  背景：    #FFFFFF，border 1px solid #BFDBFE，圆角 8px
  focus：   border #3B82F6 + 光晕
  placeholder：「例如：你是一位专业的前端工程师，请优先使用 TypeScript 回答代码问题...」
  右下角字数：「xx / 500」，12px/#9BABC5

示例引导（文本域下方）：
  「示例：」+「我是产品经理」/「请用英语回复」/「输出 JSON 格式」
  样式：    12px，蓝色链接，点击后填入文本域

「保存」蓝色按钮（右对齐）+ 「清空」ghost 按钮
```

#### 联网搜索区块

```
区块标题：「联网搜索」

开关行：
  Label：    「默认开启联网搜索」
  右侧：     Toggle 开关（40x24px，轨道颜色：off #EBF3FF / on #3B82F6，滑块白色圆形）
  说明：     「开启后，AI 在回答时会自动搜索最新信息」，12px/#9BABC5

搜索引擎行（仅开启时显示，高度过渡 220ms）：
  Label：    「首选搜索引擎」
  右侧：     Dropdown，选项：Bing / Google / 百度
```

---

### 5.6 语音设置（`/settings/voice`）

```
页面标题：「语音设置」
```

#### 语音输入区块

```
区块标题：「语音识别」

识别语言行：
  Label：    「识别语言」
  右侧：     Dropdown，选项：中文（普通话）/ English（US）/ 日本語 / 自动检测（默认）

自动发送行：
  Label：    「语音识别后自动发送」
  描述：     「识别完成后无需点击发送，自动提交消息」，12px/#9BABC5
  右侧：     Toggle 开关（默认关闭）
```

#### 语音输出区块（TTS）

```
区块标题：「语音朗读」
副标题：  「让 AI 以语音方式朗读回复内容」

开关行：
  Label：    「启用语音朗读」
  右侧：     Toggle 开关（默认关闭）

以下选项仅在开启时显示（过渡展开）：

音色行：
  Label：    「音色」
  右侧：     单选组：「标准女声」/「标准男声」/「温柔女声」
             每项右侧有「▶ 试听」按钮（14px，#3B82F6，点击播放 3s 示例）

语速行：
  Label：    「语速」
  右侧：     滑动条（0.5x / 1.0x / 1.5x / 2.0x，默认 1.0x）
```

---

### 5.7 快捷键（`/settings/shortcuts`）

```
页面标题：「快捷键」
副标题：  「自定义常用操作的键盘快捷键（桌面端）」
```

#### 快捷键列表

```
区块标题：「对话操作」

快捷键行（每行 56px）：
  布局：      左操作名称，右快捷键展示 + 修改按钮
  快捷键标签：每个按键一个 Badge
    样式：    背景 #EFF6FF，border 1px solid #BFDBFE，圆角 4px
              内边距 4px 8px，11px/500/#5A6A8A，等宽字体（Kbd 风格）

操作列表：
  发送消息          Enter（可切换为 Ctrl+Enter）
  新建对话          Ctrl + N
  搜索对话          Ctrl + K
  切换模型          Ctrl + M
  开启语音输入      Ctrl + Shift + V
  开启联网搜索      Ctrl + Shift + S
  打开设置          Ctrl + ,（逗号）
  折叠/展开侧边栏   Ctrl + B

发送消息快捷键特殊处理：
  单选切换：  「Enter 发送（Shift+Enter 换行）」/「Ctrl+Enter 发送（Enter 换行）」
  样式：      两个 RadioCard 并排，含示例说明

「恢复默认」ghost 按钮（区块底部，右对齐）
```

---

### 5.8 数据与存储（`/settings/data`）

```
页面标题：「数据与存储」
```

#### 会话数据区块

```
区块标题：「对话历史」

自动清理行：
  Label：    「自动清理旧对话」
  右侧：     Dropdown，选项：永不（默认）/ 30 天后 / 90 天后 / 180 天后
  说明：     「超出时限的对话将在每日凌晨自动删除」，12px/#9BABC5

本地缓存行：
  Label：    「已缓存数据」
  右侧：     「约 2.4 MB」（14px/#5A6A8A）+ 「清除缓存」ghost 小按钮
  点击清除：  确认 Modal「清除后历史对话仍保留，但页面图片缓存将被删除」

导出行：
  Label：    「导出全部对话」
  右侧：     「导出 JSON」按钮（Download 图标 + 文字，ghost 样式）
             「导出 Markdown」按钮（ghost 样式）
  说明：     「导出文件将包含所有对话记录，可用于备份或迁移」，12px/#9BABC5
  点击：     下载进度 Toast（Progress bar 样式，显示导出进度）
```

#### 危险操作区块

```
区块标题：「危险操作」
区块边框：  1px solid rgba(239,68,68,0.2)

清空对话行：
  Label：    「删除所有对话」
  描述：     「删除全部对话记录，不可恢复」，13px/#9BABC5
  右侧：     「清空所有对话」描边红色按钮
  点击：     确认 Modal
    正文：   「确定删除全部 XXX 条对话？此操作不可撤销。」
    输入框：  「输入 "删除" 以确认」（防误触）
    按钮：   取消 / 确认删除（红色，输入正确后激活）
```

---

### 5.9 关于元AI（`/settings/about`）

```
页面标题：「关于元AI」

内容布局（垂直居中，文字居中）：

品牌区：
  AI 头像：  64x64，圆形蓝色渐变 + 白色「Y」Logo
  名称：    「元AI」，22px/700/#1A2540
  版本：    「v1.0.0（构建 2026.06.27）」，13px/#9BABC5

链接列表区块（标准区块容器）：
  ─ 使用说明      / 新手入门指南      → 新标签打开
  ─ 更新日志      / 查看最新功能      → 新标签打开
  ─ 服务协议      / 查看完整协议      → 新标签打开
  ─ 隐私政策      / 了解数据处理方式  → 新标签打开
  ─ 问题反馈      / 提交 Bug 或建议   → 新标签打开（GitHub Issues）

每行样式（高度 52px，内边距 水平 20px）：
  左：  图标（16px，#5A6A8A）+ 项目名（14px/400/#1A2540）+ 描述（13px/#9BABC5）
  右：  ExternalLink 图标（14px，#9BABC5）
  hover：行背景 #EFF6FF（150ms ease）

技术说明（页面最底部，居中）：
  「由 Anthropic Claude · OpenAI · DeepSeek 提供 AI 能力」
  13px/#9BABC5，行高 20px
  「© 2026 元AI. All rights reserved.」
  12px/#9BABC5，上边距 8px
```

---

### 5.10 移动端设置导航

移动端无左侧导航，改为列表页 → 详情页的两级导航。

#### 设置列表页（`/settings`，移动端）

```
顶部导航栏：「设置」居中标题 + 左上角返回按钮

个人信息卡（页面顶部，非区块样式）：
  背景：      #FFFFFF，内边距 16px 20px
  布局：      左头像（48x48，圆形）+ 中间用户名/邮箱 + 右 ChevronRight
  点击：      跳转「个人资料」页

设置分组列表（标准区块容器）：
  每项：      高度 50px，左图标（16px，#5A6A8A）+ 标题（15px/#1A2540）+ 右 ChevronRight
  hover/press：行背景 #EFF6FF

分组：
  ── 偏好设置 ──
  Palette    外观与显示
  Bot        AI 偏好
  Mic        语音设置

  ── 账号 ──
  Shield     账号安全
  Keyboard   快捷键

  ── 数据 ──
  Database   数据与存储
  HelpCircle 关于元AI

退出登录（独立按钮，底部，宽度 100%，ghost 红色样式）：
  文字：      「退出登录」，#EF4444
  点击：      确认 Modal「确定退出当前账号？」→ 清除 Token → 跳转登录页
```

---

## 六、全局通用组件

### Toast 提示

```
位置：        屏幕底部居中（移动端）/ 右上角（桌面端）
宽度：        最大 360px
高度：        自适应，最小 48px
背景：        #1A2540（通用）/ #10B981（成功）/ #EF4444（错误）
文字：        14px / #FFFFFF
圆角：        8px
内边距：      12px 16px
图标：        左侧 Info/Check/X 图标，20px，白色
出现：        translateY(100%→0)，250ms cubic-bezier(0.4,0,0.2,1)
消失：        translateY(0→100%)，200ms ease-in
停留：        3s（成功/信息）/ 5s（错误，需用户手动关闭）
```

### Modal 对话框

```
遮罩：        rgba(0,0,0,0.35)，fade in 200ms
对话框：      背景 #FFFFFF，圆角 12px，阴影 0 20px 60px rgba(30,64,175,0.15)
宽度：        最大 480px（确认弹窗）/ 360px（输入弹窗）
出现：        scale(0.95→1) + opacity(0→1)，200ms ease-out
标题：        18px/600/#1A2540，内边距 24px 24px 0
内容区：      内边距 16px 24px
操作按钮行：  内边距 0 24px 24px，右对齐
              取消按钮：secondary 样式（描边）
              确认按钮：primary 蓝色（危险操作用红色 #EF4444）
```

**重命名会话** Modal 内容：

- 标题「重命名对话」
- 输入框（预填当前名称，全选状态）
- 取消 / 保存 按钮

**删除会话** Modal 内容：

- 标题「删除对话」，正文「确认删除「对话名称」？此操作不可撤销。」
- 取消 / 删除（红色按钮）

### 用户头像

```
有头像图片：  圆形裁剪，object-cover
无头像：      圆形，背景 linear-gradient(135deg, #3B82F6, #60A5FA)
              中心显示用户名首字母（大写），字体白色
              - 32px 头像：16px 字体
              - 40px 头像：18px 字体
              - 56px 头像：24px 字体

AI 头像：     同无头像样式，但显示「Y」（代表 yuanai），字体粗体
```

---

## 七、设计师注意事项

### 必须遵守

1. **禁止使用明显 border 的卡片**：内容区、侧边栏、消息区都不得出现白色圆角矩形 + 阴影的卡片（后台管理风）
2. **禁止使用纯灰色背景**（`#F5F5F5` 等）：所有背景必须带蓝色调
3. **圆角统一 8px**：不得出现 16px 以上的输入框/按钮圆角（输入框容器 14px 例外）
4. **颜色不得使用蓝紫色**（`#6366F1`、`#7C3AED`）：本项目主色为纯蓝（`#3B82F6`）
5. **认证页背景不能留白**：左侧装饰区必须有浮动元素填充
6. **停止生成按钮用红色**：`#EF4444`，与发送按钮蓝色形成明确区分，避免误操作
7. **工具调用卡不得遮挡消息**：始终在独立区域展示，不与正文混排
8. **Artifact 面板背景为纯白**：`#FFFFFF`，与对话区 `#F4F8FF` 形成对比

### 推荐实践

1. 多使用**背景色差异**替代 border 来区分区域
2. 图标使用 **Lucide** 图标集，风格统一（线条图标，2px stroke）
3. 会话列表的 hover 状态用**过渡动画**（150ms），不要跳变
4. AI 消息区域尽量**舒展**，文字行高 26px，给阅读足够呼吸感
5. 按钮 hover 时轻微上浮（`translateY(-1px)`）增加交互感
6. 语音波形使用**实时驱动**动画，不要用预置 CSS 动画，波形必须反映真实音量
7. 推荐追问标签**不要超过 3 个**，保持克制，不能干扰阅读体验
8. Tool 调用状态图标使用语义颜色：进行中蓝色、成功绿色、失败红色

### OpenDesign/Penpot 原型建议

**需要创建的页面（共 18 页）**：

| 页面名称              | 说明                                        |
| --------------------- | ------------------------------------------- |
| Login                 | 登录页，含所有校验状态                      |
| Register              | 注册页，含密码强度指示器                    |
| ForgotPassword        | 重置密码，含两步骤                          |
| Main-Empty            | 欢迎页（空状态），含能力标签和提示卡        |
| Main-Chat             | 对话中状态，含多种消息类型                  |
| Main-WithArtifact     | 展开 Artifact 面板的对话页                  |
| Main-ToolCall         | AI 正在调用工具的中间状态                   |
| Main-VoiceInput       | 语音录入激活态                              |
| Mobile-Chat           | 移动端对话页                                |
| Mobile-Drawer         | 移动端侧边栏抽屉                            |
| Settings-Profile      | 个人资料页（含头像上传、使用统计）          |
| Settings-Security     | 账号安全页（含密码修改、关联账号、注销）    |
| Settings-Appearance   | 外观与显示（主题切换、字体大小、消息密度）  |
| Settings-AI           | AI 偏好（默认模型、创意度滑块、系统提示词） |
| Settings-Voice        | 语音设置（识别语言、TTS 音色）              |
| Settings-Data         | 数据与存储（导出、清空、自动清理）          |
| Mobile-Settings-List  | 移动端设置列表页（含个人信息卡）            |
| Mobile-Settings-About | 关于元AI 页                                 |

**组件封装清单（Components）**：

- Button（primary / secondary / ghost / danger，三种尺寸）
- Input（含 Label / 图标 / 错误态 / 成功态）
- Avatar（User / AI，四种尺寸）
- ModelSelector（触发按钮 + Dropdown）
- ConversationItem（默认 / hover / active / 置顶）
- UserMessage（含图片附件 / 文档附件 / 编辑态）
- AIMessage（含 Markdown / 代码块 / 联网引用 / 推荐追问）
- ToolCallCard（搜索 / 代码执行 / 文件分析 / 图像生成，各状态）
- ArtifactPanel（代码 / 文档 / HTML 预览，展开/关闭状态）
- VoiceInputBar（录音中 / 识别中 / 静音检测）
- StopGenerateButton（生成中替换发送按钮）
- SuggestionCard（欢迎页提示卡）
- CapabilityPill（欢迎页能力标签）
- ThinkingBlock（推理过程折叠区）
- CodeBlock（含语言标签 / 复制 / 面板查看按钮）
- Toast（info / success / error）
- Modal（确认 / 输入 / 全屏 Artifact）
- SettingsContainer（Modal 容器 + 左侧导航 + 右侧内容区）
- SettingsNav（导航项，含分组标题和活跃态）
- SettingsBlock（设置区块容器，含标题行和分割线）
- SettingsRow（设置行，Label + Value/操作，含只读/编辑态）
- Toggle（开关，含 on/off/disabled 三态）
- Slider（滑动条，含刻度、浮动气泡值显示）
- RadioCard（单选卡，如主题选择 / 消息密度）
- KeyboardBadge（按键标签，Kbd 风格）
- UsageProgressBar（额度使用进度条）
- AvatarUploader（头像上传区，含 hover 蒙层和删除按钮）

**动画效果标注**：

在 Penpot 中用 **Interactions** 功能标注动画类型和时长。重点标注：

- 语音按钮激活的红色扩散光圈
- Tool 调用成功的绿色勾弹出
- Artifact 面板从右侧滑入
- 推荐追问标签逐项淡入
- 停止按钮替换发送按钮的过渡

**颜色管理**：统一用 **Library Colors** 管理，变量名与本文档 CSS 变量名保持对应
